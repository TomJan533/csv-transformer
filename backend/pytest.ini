[pytest]
DJANGO_SETTINGS_MODULE = api.settings

# Set a minimum logging level to show log output during test execution
log_cli = true
log_cli_level = INFO

# Add markers for special types of tests
markers =
    integration: mark a test as an integration test
    slow: mark a test as slow
